# Plan 1: UI Shell with Placeholder Data

Build the complete TUI with placeholder/mock data. All layout, resize handling, keybindings, and modals working — no WebSocket connection.

## Steps

### 1. Dependencies
Add bubbletea, bubbles (viewport, textinput), lipgloss to go.mod.

### 2. pkg/ui/theme.go
Define the color palette (primary amber, secondary gray, accent highlight) as a Theme struct with lipgloss.Color values and helper styles. Follow anvil's theme.go pattern.

### 3. pkg/ui/helpers.go
- `RenderSizeError(minW, minH, curW, curH, totalW, totalH) string` — centered error box
- `CalculateChatLayout(termW, termH) ChatLayout` — returns sidebar width, message pane width, heights
- `CreatePaneStyle(isActive bool, width, height int) lipgloss.Style` — thick/normal border
- `FillTerminal(content string, width, height int) string`

### 4. pkg/ui/keybindings.go
- `KeyBinding` struct, `KeyBindingSet` with `Contains(key)` and `Render()`
- No pre-built sets here — chat-specific sets live in internal/chat/keys.go

### 5. internal/chat/keys.go
Define `ChatKeyMap` with all Ctrl combo bindings. Expose `ChatKeyBindings() ui.KeyBindingSet` for help bar rendering.

### 6. internal/chat/channels.go
- `ChannelList` struct with hardcoded placeholder channels (#general, #engineering, #ops, plus DMs alice/bob)
- `cursor int`, `width/height int`
- `Update(msg)` for cursor movement
- `View()` renders list with selection highlight, "#" prefix for channels, section headers

### 7. internal/chat/messages.go
- `MessagePane` with `viewport.Model`
- Hardcoded placeholder messages per channel
- `SetChannel(name)` swaps content and resets viewport
- `AppendMessage(msg)` adds and auto-scrolls if at bottom
- `View()` renders the viewport

### 8. internal/chat/input.go
- `InputBar` wrapping `textinput.Model`
- Placeholder text "type a message..."
- `Focus()`/`Blur()` methods
- `View()` renders with border

### 9. internal/chat/msgtypes.go
Define all tea.Msg types (ChannelSelectedMsg, MessageSentMsg, etc.) even though most won't fire yet. This sets up the contract for Plan 2.

### 10. internal/chat/model.go
- `ChatModel` with all child components, activePane, tooSmall flag
- `Init()` returns nil (no commands needed with placeholders)
- `Update()`: WindowSizeMsg (check min, propagate), KeyMsg (dispatch by pane/binding), modal toggling
- `View()`: if tooSmall render size error, else compose sidebar + message pane + input + help bar
- Modal overlay rendering for channel create and user invite

### 11. internal/chat/modals.go
- `ChannelCreateModal` — text input for channel name, public/private toggle, confirm/cancel
- `UserInviteModal` — text input for username, confirm/cancel
- Both render as centered overlays on top of the chat UI

### 12. cmd/chat/chat.go
- `NewChatCmd()` returning *cobra.Command
- Flags: `--sharkfin-host`, `--username`
- RunE: create model with placeholders, run tea.NewProgram with AltScreen
- No client connection yet

### 13. cmd/root.go
Register the chat subcommand via `rootCmd.AddCommand(chat.NewChatCmd())`

### 14. Verify
- `mise run build` succeeds
- `mise run lint` passes
- Binary launches, renders correctly
- Resize works, minimum size error works
- All keybindings navigate correctly
- Modals open/close
