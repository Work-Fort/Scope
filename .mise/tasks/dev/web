#!/usr/bin/env bash
# SPDX-License-Identifier: Apache-2.0
#MISE description="Start Vite dev server for shell SPA"
set -euo pipefail

if [ ! -d "web/shell/node_modules" ]; then
  echo "Run 'cd web && pnpm install' first" >&2
  exit 1
fi

cd web/shell && pnpm dev
