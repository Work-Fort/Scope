#!/usr/bin/env bash
# SPDX-License-Identifier: Apache-2.0
#MISE description="Build all Rust crates (dev)"
#MISE sources=["crates/**/*.rs", "src-tauri/**/*.rs", "Cargo.toml", "Cargo.lock"]
set -euo pipefail

cargo build --workspace
echo "Built all workspace crates (dev)"
